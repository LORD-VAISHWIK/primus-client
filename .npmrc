# Platform-specific dependencies
target_platform=linux
target_arch=x64

# Ensure proper resolution of native modules
node-linker=hoisted

# Disable optional dependencies that might cause issues
optional=false

# Use npm's legacy peer deps resolution
legacy-peer-deps=true
